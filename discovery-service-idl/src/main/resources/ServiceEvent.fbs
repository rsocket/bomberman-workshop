include "ServiceInfo.fbs";

namespace xyz.bomberman.discovery.data;

enum EventType: byte { Connected = 0, Disconnected = 1 }

table ServiceEvent {
  type: EventType;
  serviceInfo: ServiceInfo;
}

root_type ServiceEvent;
